/* Simplified Clean UI */
body {
    text-align: center;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
    background: #f5f5f5;
    color: #333;
    min-height: 100vh;
    margin: 0;
    padding: 20px;
}

.container {
    max-width: 500px;
    margin: 0 auto;
    background: white;
    border-radius: 12px;
    padding: 24px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

h1 {
    margin: 0 0 20px 0;
    font-size: 1.8em;
    font-weight: 600;
    color: #1a1a1a;
}

.game-info {
    margin-bottom: 16px;
}

#board {
    display: grid;
    gap: 8px;
    justify-content: center;
    margin: 20px auto;
    max-width: 360px;
}

.cell {
    width: 70px;
    height: 70px;
    background: #fff;
    color: #333;
    font-size: 32px;
    font-weight: 600;
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    border: 2px solid #e0e0e0;
    border-radius: 8px;
    transition: all 0.2s ease;
}

.cell:hover:not(.taken) {
    background: #f0f0f0;
    border-color: #bdbdbd;
}

.cell.taken {
    cursor: not-allowed;
}

#info {
    margin-top: 16px;
    font-size: 16px;
    min-height: 20px;
}

button {
    margin-top: 12px;
    padding: 10px 20px;
    background: #4caf50;
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-weight: 500;
    font-size: 14px;
    transition: background 0.2s ease;
}

button:hover {
    background: #45a049;
}

.player-info {
    display: flex;
    justify-content: space-around;
    margin-bottom: 16px;
    padding: 12px;
    background: #f8f8f8;
    border-radius: 8px;
}

.player {
    text-align: center;
}

.player.current-turn {
    background: #e3f2fd;
    border-radius: 6px;
    padding: 8px 12px;
}

.player-symbol {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 4px;
}

.player-name {
    font-size: 13px;
    color: #666;
}

.winner-message {
    color: #4caf50;
    font-size: 20px;
    font-weight: 600;
}

.draw-message {
    color: #ff9800;
    font-size: 20px;
    font-weight: 600;
}

.waiting-message {
    color: #2196f3;
    font-size: 16px;
}

/* Multi-Level Game Styles - Simplified */
.multi-level-info {
    background: #f8f8f8;
    padding: 12px 16px;
    border-radius: 8px;
    margin-bottom: 16px;
}

.level-indicator {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-weight: 500;
}

#currentLevelDisplay {
    font-size: 16px;
}

.levels-won {
    font-size: 13px;
}

.level-score {
    margin: 0 4px;
    padding: 4px 8px;
    background: white;
    border-radius: 4px;
}

.multi-level-result {
    margin-top: 8px;
    font-size: 14px;
    padding: 8px;
    background: #f8f8f8;
    border-radius: 6px;
}

/* Time Display Styles - Simplified */
.time-display-container {
    margin: 12px 0;
    padding: 10px;
    background: #f8f8f8;
    border-radius: 6px;
    font-size: 13px;
}

.time-display {
    margin: 0 6px;
    padding: 4px 10px;
    background: white;
    border-radius: 4px;
    display: inline-block;
}

.time-result {
    margin-top: 8px;
    font-size: 14px;
    padding: 8px;
    background: #fff3cd;
    border-radius: 6px;
    color: #856404;
    font-weight: 500;
}